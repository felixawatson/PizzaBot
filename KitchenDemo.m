classdef KitchenDemo < handle
    %%Kitchen Environment
    %sets up the static environment that the robots will be operating in
    %and runs the simulation demo
    properties
    
    end

    methods
        %constructor
        function self = KitchenDemo()
        end

        % adds all surfaces and objects into the evironment
        function buildEnv(self)
        end
    end
end